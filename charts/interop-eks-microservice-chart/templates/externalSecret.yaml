{{- if and .Values.externalSecrets .Values.externalSecrets.container .Values.externalSecrets.container.create }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "interop-eks-microservice-chart.labels" . | nindent 4 }}
    {{- if .Values.externalSecrets.container.labels }}
    {{- toYaml .Values.externalSecrets.container.labels | nindent 4 }}
    {{- end }}
  {{- if .Values.externalSecrets.container.annotations }}
  annotations:
    {{- toYaml .Values.externalSecrets.container.annotations | nindent 4 }}
  {{- end }}
spec:
  refreshPolicy: {{ .Values.externalSecrets.container.refreshPolicy }}
  refreshInterval: {{ .Values.externalSecrets.container.refreshInterval | quote }}
  secretStoreRef:
    {{- if .Values.externalSecrets.container.secretStoreRef }}
    name: {{ include "interop-eks-microservice-chart.render-template" (dict "value" .Values.externalSecrets.container.secretStoreRef.name "context" $) }}
    kind: {{ .Values.externalSecrets.container.secretStoreRef.kind | default "SecretStore" }}
    {{- else }}
    {{- fail "secretStoreRef is required for ExternalSecret" }}
    {{- end }}
  target:
    name: {{ include "interop-eks-microservice-chart.render-template" (dict "value" (.Values.externalSecrets.container.targetSecret.name | default .Values.name) "context" $) }}
    creationPolicy: {{ .Values.externalSecrets.container.targetSecret.creationPolicy }}
    deletionPolicy: {{ .Values.externalSecrets.container.targetSecret.deletionPolicy }}
    {{- if .Values.externalSecrets.container.data }}
    template:
      type: Opaque
      metadata:
        annotations:
          externalsecret/secret-applied-hash: {{ include "externalsecrets.contractMarker" (dict "data" .Values.externalSecrets.container.data) }}
    {{- end }}
  {{- if .Values.externalSecrets.container.data }}
  data:
    {{- range .Values.externalSecrets.container.data }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        {{- $key := include "interop-eks-microservice-chart.render-template" (dict "value" .remoteRef.key "context" $) }}
        key: {{ $key }}
        {{- if .remoteRef.property }}
        property: {{ include "interop-eks-microservice-chart.render-template" (dict "value" .remoteRef.property "context" $) }}
        {{- end }}
        {{- if .remoteRef.version }}
        version: {{ .remoteRef.version }}
        {{- end }}
        {{- if .remoteRef.conversionStrategy }}
        conversionStrategy: {{ .remoteRef.conversionStrategy }}
        {{- end }}
        {{- if .remoteRef.decodingStrategy }}
        decodingStrategy: {{ .remoteRef.decodingStrategy }}
        {{- end }}
    {{- end }}
  {{- end }}
{{- end }}
