{{- if and .Values.externalSecrets .Values.externalSecrets.app .Values.externalSecrets.app.create }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "interop-eks-microservice-chart.labels" . | nindent 4 }}
    {{- if .Values.externalSecrets.app.labels }}
    {{- toYaml .Values.externalSecrets.app.labels | nindent 4 }}
    {{- end }}
  {{- if .Values.externalSecrets.app.annotations }}
  annotations:
    {{- toYaml .Values.externalSecrets.app.annotations | nindent 4 }}
  {{- end }}
spec:
  refreshPolicy: {{ .Values.externalSecrets.app.refreshPolicy }}
  refreshInterval: {{ .Values.externalSecrets.app.refreshInterval | quote }}
  secretStoreRef:
    {{- if .Values.externalSecrets.app.secretStoreRef }}
    name: {{ include "interop-eks-microservice-chart.render-template" (dict "value" .Values.externalSecrets.app.secretStoreRef.name "context" $) }}
    kind: {{ .Values.externalSecrets.app.secretStoreRef.kind | default "SecretStore" }}
    {{- else }}
    {{- fail "secretStoreRef is required for ExternalSecret" }}
    {{- end }}
  target:
    name: {{ include "interop-eks-microservice-chart.render-template" (dict "value" (.Values.externalSecrets.app.targetSecret.name | default .Values.name) "context" $) }}
    creationPolicy: {{ .Values.externalSecrets.app.targetSecret.creationPolicy }}
    deletionPolicy: {{ .Values.externalSecrets.app.targetSecret.deletionPolicy }}
    {{- if .Values.externalSecrets.app.data }}
    template:
      type: Opaque
      metadata:
        annotations:
          externalsecret/secret-applied-hash: {{ include "externalsecrets.contractMarker" (dict "data" .Values.externalSecrets.app.data) }}
    {{- end }}
  {{- if .Values.externalSecrets.app.data }}
  data:
    {{- range .Values.externalSecrets.app.data }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        {{- $key := include "interop-eks-microservice-chart.render-template" (dict "value" .remoteRef.key "context" $) }}
        key: {{ $key }}
        {{- if .remoteRef.property }}
        property: {{ include "interop-eks-microservice-chart.render-template" (dict "value" .remoteRef.property "context" $) }}
        {{- end }}
        {{- if .remoteRef.version }}
        version: {{ .remoteRef.version }}
        {{- end }}
        {{- if .remoteRef.conversionStrategy }}
        conversionStrategy: {{ .remoteRef.conversionStrategy }}
        {{- end }}
        {{- if .remoteRef.decodingStrategy }}
        decodingStrategy: {{ .remoteRef.decodingStrategy }}
        {{- end }}
    {{- end }}
  {{- end }}
{{- end }}
