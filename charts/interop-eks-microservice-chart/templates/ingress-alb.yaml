{{- if and .Values.ingress .Values.ingress.create .Values.service.targetGroupArn }}
  {{ fail "ingress.create and service.targetGroupArn cannot both be configured; please define just one." }}
{{- end }}

{{- if and .Values.ingress.create (eq .Values.ingress.type "alb") }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "interop-eks-microservice-chart.labels" . | nindent 4 }}
  annotations:
    alb.ingress.kubernetes.io/scheme: "internal"
    alb.ingress.kubernetes.io/target-type: "ip"
    alb.ingress.kubernetes.io/group.name: {{ .Values.ingress.groupName | quote }}
    alb.ingress.kubernetes.io/load-balancer-attributes: "routing.http.preserve_host_header.enabled=true,routing.http.xff_header_processing.mode=preserve"
    {{- if not (kindIs "invalid" .Values.ingress.groupOrder) }}  {{/* GroupOrder is an integer and can be 0; this kind check is required otherwise the if condition evaluates to false when groupOrder is set to 0 */}}
    alb.ingress.kubernetes.io/group.order: {{ .Values.ingress.groupOrder }}
    {{- end }}
spec:
  ingressClassName: "alb"
  rules:
    {{- if .Values.ingress.applicationPath }}
    - http:
        paths:
          - path: {{ .Values.ingress.applicationPath | trimSuffix "/" }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.name }}
                port:
                  number: {{ .Values.service.containerPort }}
    {{- if .Values.ingress.host }}
      host: {{ include "interop-eks-microservice-chart.render-template" (dict "value" .Values.ingress.host "context" $) | quote }}
    {{- end }}
    {{- end }}
{{- end }}
