{{- if and .Values.externalSecrets .Values.externalSecrets.flywayInitContainer .Values.externalSecrets.flywayInitContainer.create }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.name }}-flyway
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "interop-eks-microservice-chart.labels" . | nindent 4 }}
    {{- if .Values.externalSecrets.flywayInitContainer.labels }}
    {{- toYaml .Values.externalSecrets.flywayInitContainer.labels | nindent 4 }}
    {{- end }}
  {{- if .Values.externalSecrets.flywayInitContainer.annotations }}
  annotations:
    {{- toYaml .Values.externalSecrets.flywayInitContainer.annotations | nindent 4 }}
  {{- end }}
spec:
  refreshPolicy: {{ .Values.externalSecrets.flywayInitContainer.refreshPolicy }}
  refreshInterval: {{ .Values.externalSecrets.flywayInitContainer.refreshInterval | quote }}
  secretStoreRef:
    {{- if .Values.externalSecrets.flywayInitContainer.secretStoreRef }}
    name: {{ include "interop-eks-microservice-chart.render-template" (dict "value" .Values.externalSecrets.flywayInitContainer.secretStoreRef.name "context" $) }}
    kind: {{ .Values.externalSecrets.flywayInitContainer.secretStoreRef.kind | default "SecretStore" }}
    {{- else }}
    {{- fail "secretStoreRef is required for ExternalSecret" }}
    {{- end }}
  target:
    name: {{ include "interop-eks-microservice-chart.render-template" (dict "value" (.Values.externalSecrets.flywayInitContainer.targetSecret.name | default (printf "%s-flyway" .Values.name)) "context" $) }}
    creationPolicy: {{ .Values.externalSecrets.flywayInitContainer.targetSecret.creationPolicy }}
    deletionPolicy: {{ .Values.externalSecrets.flywayInitContainer.targetSecret.deletionPolicy }}
    {{- if .Values.externalSecrets.flywayInitContainer.data }}
    template:
      type: Opaque
      metadata:
        annotations:
          externalsecret/secret-applied-hash: {{ include "externalsecrets.contractMarker" (dict "data" .Values.externalSecrets.flywayInitContainer.data) }}
    {{- end }}
  {{- if .Values.externalSecrets.flywayInitContainer.data }}
  data:
    {{- range .Values.externalSecrets.flywayInitContainer.data }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        {{- $key := include "interop-eks-microservice-chart.render-template" (dict "value" .remoteRef.key "context" $) }}
        key: {{ $key }}
        {{- if .remoteRef.property }}
        property: {{ include "interop-eks-microservice-chart.render-template" (dict "value" .remoteRef.property "context" $) }}
        {{- end }}
        {{- if .remoteRef.version }}
        version: {{ .remoteRef.version }}
        {{- end }}
        {{- if .remoteRef.conversionStrategy }}
        conversionStrategy: {{ .remoteRef.conversionStrategy }}
        {{- end }}
        {{- if .remoteRef.decodingStrategy }}
        decodingStrategy: {{ .remoteRef.decodingStrategy }}
        {{- end }}
    {{- end }}
  {{- end }}
{{- end }}
