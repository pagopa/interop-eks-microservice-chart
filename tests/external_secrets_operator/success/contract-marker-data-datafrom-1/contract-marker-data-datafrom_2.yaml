externalSecrets:
  create: true
  data:
    - secretKey: USERNAME
      remoteRef:
        key: my-secret
        property: username
        version: v2
  dataFrom:
    - extract:
        key: shared-secret
        version: v1