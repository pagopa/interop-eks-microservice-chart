# only datafrom change
externalSecrets:
  create: true
  data:
    - secretKey: USERNAME
      remoteRef:
        key: my-secret
        property: username
        version: v1
  dataFrom:
    - extract:
        key: shared-secret
        version: v2