# extract, find and sourceRef all in the same entry
externalSecrets:
  dataFrom:
  - extract:
      key: my-app/config
    find:
      name:
        regexp: "^my-app/.*"
    sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: Password
        name: my-generator