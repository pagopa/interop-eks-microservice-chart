# rewrite on sourceRef entry (not supported)
externalSecrets:
  dataFrom:
  - sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: Password
        name: my-generator
    rewrite:
      - regexp:
          source: "(.*)"
          target: "$1"
