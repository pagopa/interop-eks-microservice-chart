# find and sourceRef in the same entry
externalSecrets:
  dataFrom:
  - find:
      name:
        regexp: "^my-app/.*"
    sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: Password
        name: my-generator