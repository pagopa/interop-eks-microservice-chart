# extract and sourceRef in the same entry
externalSecrets:
  dataFrom:
  - extract:
      key: my-app/config
    sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: Password
        name: my-generator