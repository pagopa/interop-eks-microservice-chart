externalSecrets:
  create: true
  secretStoreRef:
    name: cluster-secret-store
    kind: ClusterSecretStore
  targetSecret:
    name: fallback-app-secret
  data:
    - secretKey: token
      remoteRef:
        key: "/apps/{{ .Values.name }}/token"
