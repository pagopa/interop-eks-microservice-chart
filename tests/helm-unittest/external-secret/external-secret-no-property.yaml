externalSecrets:
  app:
    create: true
    secretStoreRef:
      name: secret-store
      kind: SecretStore
    targetSecret:
      name: fallback-app-secret
    data:
      - secretKey: token
        remoteRef:
          key: "/apps/{{ .Values.name }}/token"
