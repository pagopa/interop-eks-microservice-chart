externalSecrets:
  app:
    create: true
    secretStoreRef:
      name: cluster-secret-store
      kind: ClusterSecretStore
    targetSecret:
      name: fallback-app-secret
    data:
      - secretKey: KEY_1
        remoteRef:
          key: "/apps/{{ .Values.name }}/credentials"
          property: sample-value
