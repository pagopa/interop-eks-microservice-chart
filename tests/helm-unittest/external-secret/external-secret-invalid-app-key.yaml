externalSecrets:
  app:
    create: true
    targetSecret:
      name: fallback-app-secret
    data:
      - secretKey: this-is-a-secret
        remoteRef:
          key: "/apps/{{ .Values.name }}/token"
          property: token
