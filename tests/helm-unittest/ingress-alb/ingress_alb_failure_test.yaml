suite: ingress alb failures
templates:
  - templates/ingress-alb.yaml

tests:
  - it: fails schema validation when the host type is invalid
    values:
      - ingress-alb-values-base.yaml
      - fail-case-1-invalid-host-type.yaml
    asserts:
      - failedTemplate: {}
          #errorPattern: "at '/ingress/host': got number, want null or string"
