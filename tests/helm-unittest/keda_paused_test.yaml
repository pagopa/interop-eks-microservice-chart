suite: keda paused
templates:
  - templates/scaledobject.yaml

tests:
  - it: renders a ScaledObject with paused annotation set to true
    values:
      - ../values/keda-paused/values-base.yaml
      - ../values/keda-paused/paused-true.yaml
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: ScaledObject
      - equal:
          path: metadata.annotations["autoscaling.keda.sh/paused"]
          value: "true"
      - equal:
          path: spec.scaleTargetRef.name
          value: test-keda-paused

  - it: renders a ScaledObject with paused annotation set to false
    values:
      - ../values/keda-paused/values-base.yaml
      - ../values/keda-paused/paused-false.yaml
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: metadata.annotations["autoscaling.keda.sh/paused"]
          value: "false"
      - equal:
          path: spec.minReplicaCount
          value: 1
      - equal:
          path: spec.maxReplicaCount
          value: 2

  - it: defaults paused annotation to false when omitted
    values:
      - ../values/keda-paused/values-base.yaml
      - ../values/keda-paused/paused-omitted.yaml
    asserts:
      - hasDocuments:
          count: 1
      - equal:
          path: metadata.annotations["autoscaling.keda.sh/paused"]
          value: "false"
      - equal:
          path: spec.triggers[0].type
          value: cpu
      - equal:
          path: spec.triggers[0].metadata.value
          value: "50"
