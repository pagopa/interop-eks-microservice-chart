# Fail case 2 – Missing required field: ingressClassName
# Expected: helm lint fails with a schema validation error because ingress.ingressClassName is required when ingress.create is true.
name: test
namespace: test
techStack: nodejs

deployment:
  image:
    repository: nginx
    tag: stable

ingress:
  create: true
  type: nginx
  rules:
    - host: api.example.com
      path: /api
      pathType: Prefix
