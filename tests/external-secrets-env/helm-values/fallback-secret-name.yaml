externalSecrets:
  create: true
  targetSecret:
    name: ""
  secretStoreRef:
    name: app-secret-store
    kind: SecretStore
  data:
    - secretKey: JWT_SECRET
      remoteRef:
        key: /app/secrets
        property: jwt-secret
